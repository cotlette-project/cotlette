"""
Cotlette settings for {{ project_name }} project.

Generated by 'cotlette startproject' using Cotlette {{ cotlette_version }}.

For more information on this file, see
https://docs.cotletteproject.com/en/{{ docs_version }}/topics/settings/

For the full list of settings and their values, see
https://docs.cotletteproject.com/en/{{ docs_version }}/ref/settings/
"""

from pathlib import Path

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent


# Quick-start development settings - unsuitable for production
# See https://docs.cotletteproject.com/en/{{ docs_version }}/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = '{{ secret_key }}'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = []


# Application definition

INSTALLED_APPS = [
    'cotlette.contrib.admin',
    'cotlette.contrib.auth',
    'cotlette.contrib.contenttypes',
    'cotlette.contrib.sessions',
    'cotlette.contrib.messages',
    'cotlette.contrib.staticfiles',
]

MIDDLEWARE = [
    'cotlette.middleware.security.SecurityMiddleware',
    'cotlette.contrib.sessions.middleware.SessionMiddleware',
    'cotlette.middleware.common.CommonMiddleware',
    'cotlette.middleware.csrf.CsrfViewMiddleware',
    'cotlette.contrib.auth.middleware.AuthenticationMiddleware',
    'cotlette.contrib.messages.middleware.MessageMiddleware',
    'cotlette.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = '{{ project_name }}.urls'

TEMPLATES = [
    {
        'BACKEND': 'cotlette.template.backends.cotlette.CotletteTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'cotlette.template.context_processors.request',
                'cotlette.contrib.auth.context_processors.auth',
                'cotlette.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = '{{ project_name }}.wsgi.application'


# Database
# https://docs.cotletteproject.com/en/{{ docs_version }}/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'cotlette.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}


# Password validation
# https://docs.cotletteproject.com/en/{{ docs_version }}/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'cotlette.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'cotlette.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'cotlette.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'cotlette.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.cotletteproject.com/en/{{ docs_version }}/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.cotletteproject.com/en/{{ docs_version }}/howto/static-files/

STATIC_URL = 'static/'

# Default primary key field type
# https://docs.cotletteproject.com/en/{{ docs_version }}/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'cotlette.db.models.BigAutoField'
